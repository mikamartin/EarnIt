# tools/

Asset pipeline scripts. Run from the repo root.

| Script | Purpose |
|---|---|
| `prep_mascot.py` | Resize a drawable PNG to 512×512 with max PNG compression. Requires Pillow (`pip install pillow`). |
