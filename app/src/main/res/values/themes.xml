<resources>
    <style name="Theme.EarnIt" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
        <item name="android:windowSplashScreenBackground">@color/splash_icon_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>

    <!-- Widget utility activities: same splash, no action bar -->
    <style name="Theme.EarnIt.Widget" parent="Theme.EarnIt">
        <item name="android:windowSplashScreenBackground">@color/splash_icon_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>
</resources>
